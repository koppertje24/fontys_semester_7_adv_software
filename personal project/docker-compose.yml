version: '1.2'

name: personal_project

services:
  # een PostgreSQL database for Kong om routes and configurations op te slaan
  kong-database:
    image: postgres:13
    container_name: kong-database
    environment:
      POSTGRES_USER: kong
      POSTGRES_DB: kong
      POSTGRES_PASSWORD: kong
    networks:
      - kong-net
    volumes:
      - kong-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "kong"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Run Kong migrations om de database te initialiseren
  kong-migration:
    image: kong:latest
    container_name: kong-migration
    command: kong migrations bootstrap
    environment:
      KONG_DATABASE: postgres
      KONG_PG_HOST: kong-database
      KONG_PG_USER: kong
      KONG_PG_PASSWORD: kong
    networks:
      - kong-net
    depends_on:
      kong-database:
        condition: service_healthy
    restart: on-failure

  # de Kong Gateway service zelf die API requests afhandelt
  kong:
    image: kong:latest
    container_name: kong-gateway
    environment:
      KONG_DATABASE: postgres
      KONG_PG_HOST: kong-database
      KONG_PG_USER: kong
      KONG_PG_PASSWORD: kong
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_ADMIN_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_ADMIN_LISTEN: 0.0.0.0:8001
      KONG_ADMIN_GUI_URL: http://localhost:8002
    ports:
      - "8000:8000"   # de Proxy port (HTTP)
      - "8443:8443"   # de Proxy port (HTTPS)
      - "8001:8001"   # de Admin API
      - "8444:8444"   # de Admin API (HTTPS)
    networks:
      - kong-net
      - back-network
    depends_on:
      kong-database:
        condition: service_healthy
      kong-migration:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "kong", "health"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Kong Decker voor de sync configuratie naar en van Kong Gateway
  deck-sync:
    image: kong/deck:latest
    container_name: deck-sync
    command: gateway sync /deck/kong.yaml --kong-addr http://kong-gateway:8001
    volumes:
      - ./kong.yaml:/deck/kong.yaml
    networks:
      - kong-net
    depends_on:
      kong:
        condition: service_healthy
    restart: on-failure

  # Konga, een webgebaseerde GUI voor het beheren van Kong
  konga:
    image: pantsel/konga:latest
    container_name: konga
    environment:
      NODE_ENV: development
    ports:
      - "1337:1337"
    networks:
      - kong-net
      - back-network
    depends_on:
      kong:
        condition: service_healthy

  # een test backend service die door Kong kan worded beheerd
  hls-backend:
    build:
      context: ./test_hls_service
    container_name: hls-backend
    ports:
      - "3000:3000"
    networks:
      - back-network

# netwerken voor isolatie tussen de buiten api en de services
networks:
  kong-net:
    driver: bridge
  back-network:
    driver: bridge

# volumes voor het persistent opslaan van de database data
volumes:
  kong-db-data: